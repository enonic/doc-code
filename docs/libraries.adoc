= Platform libraries
include::.variables.adoc[]
:toc: right
:imagesdir: images
This section documents the standard JavaScript libraries shipped with the Enonic XP platform.

== Introduction

When building Enonic apps you may access a broad set of platform capabilities by requiring these libraries. Each library is pulled into your app as a `build.gradle` dependency and loaded at runtime via `require('/lib/xp/<library>')`.

A library can be loaded two ways — both fully typed once the `@enonic-types` packages are installed (see <<fundamentals/typescript#, TypeScript>>):

.require — CommonJS, the standard form
[source,typescript]
----
const authLib = require('/lib/xp/auth');
const user = authLib.getUser();
----

.ES6 import — also needs a `paths` mapping in `tsconfig.json`
[source,typescript]
----
import { getUser } from '/lib/xp/auth';
const user = getUser();
----


== Headless APIs

Visit the https://developer.enonic.com/docs/cms/xp8[CMS documentation] for details regarding the content schemas and our headless GraphQL API

== JavaDoc

Java developers or anyone interested in a Platform deep dive may check out our JavaDoc (https://repo.enonic.com/public/com/enonic/xp/docs/{version}/docs-{version}-javadoc.zip[zipfile download]).


